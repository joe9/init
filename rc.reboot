#!/bin/sh

PATH=/sbin:/bin:/usr/sbin:/usr/bin

/bin/sync && /bin/sync && /bin/sync

# rc-service xstart stop
RUNLEVEL=6 /sbin/rc reboot

# to check out any messages
/usr/bin/sleep 5

# vim:filetype=sh tabstop=3 shiftwidth=3 expandtab
